rule: spec/v1alpha5/01-emision-graphql.md#2.8.1
level: L0
expects: structure
format: graphql
summary: >
  Una `Function` con endoso `humanApproval` NO puede devolver su `output`: en el
  instante de la llamada el acto no ha ocurrido. Devuelve `ApprovalRequired`.
