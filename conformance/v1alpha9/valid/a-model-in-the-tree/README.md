# v1alpha9 / valid / a-model-in-the-tree

**Regla:** [`01-model.md` 3](../../../../spec/v1alpha9/01-model.md#3) · **Nivel:** L0

---

La forma minima de esta version: `profile`, `tier` y `task`, y nada mas. No hay runtime, no hay recursos, no hay pesos — todo eso es del perfil. Y compila solo: un modelo declarado que ninguna funcion invoca todavia es exactamente lo que `POST /modelos` deja en el arbol.
