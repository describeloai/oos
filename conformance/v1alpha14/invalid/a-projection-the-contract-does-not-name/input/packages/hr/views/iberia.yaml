apiVersion: oos.dev/v1alpha14
kind: View
metadata: { name: iberia, namespace: hr }
spec:
  owner: team:hr
  dialect: duckdb
  sql: |
    SELECT id, pais FROM hr.empleados
  columns:
    id: { type: String }
