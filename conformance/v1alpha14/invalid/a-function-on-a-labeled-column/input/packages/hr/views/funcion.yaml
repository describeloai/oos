apiVersion: oos.dev/v1alpha14
kind: View
metadata: { name: funcion, namespace: hr }
spec:
  owner: team:hr
  dialect: duckdb
  sql: |
    SELECT id FROM hr.empleados WHERE substr(dni, 1, 1) = '1'
  columns:
    id: { type: String }
