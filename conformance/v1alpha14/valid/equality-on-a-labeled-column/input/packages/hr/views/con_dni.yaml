apiVersion: oos.dev/v1alpha14
kind: View
metadata: { name: con_dni, namespace: hr }
spec:
  owner: team:hr
  dialect: duckdb
  sql: |
    SELECT id FROM hr.empleados WHERE dni IS NOT NULL AND dni NOT IN ('0', '1')
  columns:
    id: { type: String }
