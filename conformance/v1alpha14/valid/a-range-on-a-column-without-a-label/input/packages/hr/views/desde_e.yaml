apiVersion: oos.dev/v1alpha14
kind: View
metadata: { name: desde_e, namespace: hr }
spec:
  owner: team:hr
  dialect: duckdb
  sql: |
    SELECT id, pais FROM hr.empleados WHERE pais >= 'E'
  columns:
    id: { type: String }
    pais: { type: String }
