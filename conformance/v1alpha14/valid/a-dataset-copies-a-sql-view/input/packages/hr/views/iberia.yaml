apiVersion: oos.dev/v1alpha14
kind: View
metadata: { name: iberia, namespace: hr }
spec:
  owner: team:hr
  dialect: duckdb
  sql: |
    SELECT id, dni FROM hr.empleados WHERE pais IN ('ES', 'PT')
  columns:
    id: { type: String }
    dni: { type: String }
