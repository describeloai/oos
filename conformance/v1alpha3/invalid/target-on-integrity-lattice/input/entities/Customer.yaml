apiVersion: oos.dev/v1alpha1
kind: Entity
metadata: { name: Customer, namespace: customers, labels: { acme.assurance: reviewed } }
spec:
  nature: entity
  primaryKey: [customerId]
  properties:
    customerId: { type: String }
    taxId:
      type: String
      labels: { gdpr.sensitivity: high }
