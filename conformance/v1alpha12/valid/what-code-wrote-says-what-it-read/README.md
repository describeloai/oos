# v1alpha12 / valid / what-code-wrote-says-what-it-read

**Regla:** [`01-dataset.md` 5](../../../../spec/v1alpha12/01-dataset.md#5) · **Nivel:** L0

---

El gemelo válido de `what-code-wrote-carries-what-it-read`: `resumen` declara `derivedFrom: [hr.iberia]`, lleva `high` en `pais` y `n`, y `resumenCopia` copia por un conducto que admite `high`. Compila. Lo que se afirma: `derivedFrom` es una clave del escrito (con `from` sería `OOS1004`), sus nombres resuelven, y la carga que trae baja por la cadena como la de cualquier raíz.
