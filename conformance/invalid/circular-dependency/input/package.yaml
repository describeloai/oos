apiVersion: oos.dev/v1alpha1
kind: Package
metadata: { name: hr, version: 1.0.0, status: active, domain: people }
spec:
  owner: team:people
  dependencies:
    # El ciclo lo cierra el lock; ver ontology.lock.
    - { package: acme/beta, version: "^1.0" }
