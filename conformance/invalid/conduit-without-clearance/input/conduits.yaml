apiVersion: oos.dev/v1alpha1
kind: ConduitPolicy
metadata: { name: default }
spec:
  conduits:
    # Solo se autoriza la caché. `materialization.topology` no aparece, y por tanto
    # su autorización es ⊥: no admite nada (P4).
    materialization.payload:
      gdpr.sensitivity: low
