apiVersion: oos.dev/v1alpha1
kind: Lattice
metadata: { name: sensitivity, namespace: gdpr }
spec:
  # `critical` se anadio despues de generar el esquema Cedar.
  levels: [none, low, high, critical]
  join: max
