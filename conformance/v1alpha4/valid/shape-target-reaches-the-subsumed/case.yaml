rule: spec/v1alpha4/03-interface.md#4.2
level: L0
expects: accept
summary: >
  La entidad declara implementar `Employee` y nunca nombra `Party`. Una regla sobre
  `Party` la alcanza igual, porque `Party.requires` esta contenido en el de `Employee`.
