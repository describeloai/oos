apiVersion: oos.dev/v1alpha4
kind: Entity
metadata: { name: Customer, namespace: crm }
spec:
  nature: entity
  primaryKey: [id]
  implements: [acme.Contactable, acme.Party]
  properties:
    id: { type: String }
    email: { is: acme.personalEmail }
    name: { is: acme.legalName }
