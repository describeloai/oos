apiVersion: oos.dev/v1alpha1
kind: Entity
metadata: { name: Customer, namespace: customers, labels: { acme.assurance: attested } }
spec:
  nature: entity
  primaryKey: [customerId]
  properties:
    customerId: { type: String }
    taxId:      { type: String }
    legalName:
      type: String
      labels: { gdpr.sensitivity: medium }
