apiVersion: oos.dev/v1alpha1
kind: Entity
metadata: { name: PurchaseOrder, namespace: scm }
spec:
  nature: entity
  primaryKey: [orderId]
  properties:
    orderId: { type: String }
    status:
      type: String
      labels: { acme.assurance: reviewed }
    total:   { type: "Money<EUR, 2>" }
    taxRate: { type: Integer }
    totalWithTax:
      type: "Money<EUR, 2>"
      # Sin etiqueta: una propiedad derivada que la declarase sería OOS4008,
      # y eso taparía el error que este caso afirma.
      derivedFrom: [scm.PurchaseOrder.total, scm.PurchaseOrder.taxRate]
